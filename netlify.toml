[build]
  base    = "."
  publish = "_site"
  command = "npm install && jekyll build && npm run minify"
[context.deploy-preview]
  command = "npm install && jekyll build && npm run minify && echo -e \"\nDisallow: /\" >> _site/robots.txt"
